version: 2

models:
  - name: transactions
    columns:
      - name: transaction_id
      - name: date
      - name: amount
      - name: description
      - name: currency
      - name: account

  - name: transactions_category_guesses
    config:
      packages:
        - pandas
        - textblob